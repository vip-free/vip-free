# NYEXSOBATEN Shortener
Simple URL shortener built with Next.js + Upstash Redis.

## Setup
1. Copy `.env.example` → `.env.local` and fill with your credentials.
2. Run `npm install`
3. Run `npm run dev` for local dev.
